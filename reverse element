#include <stdio.h>
int main()
{
 int i;
 int arr[]={2,3,4,5};
 int length=sizeof(arr)/sizeof(arr[0]);
printf("orginal array");
for(i=0;i<length;i++)
{
    scanf("%d",&arr[i]);
}
printf("reversing %d elements",length);
for(i=length-1;i>=0;i--)
{
    printf("%d",arr[i]);
}
return 0;
}
